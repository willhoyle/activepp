CMAKE_MINIMUM_REQUIRED(VERSION 3.2)

PROJECT( activepp )

IF (NOT ACTIVETICK_FOLDER_PATH)
  SET(ACTIVETICK_FOLDER_PATH "${PROJECT_SOURCE_DIR}/external/atfeed-cppsdk")
ENDIF()

ADD_SUBDIRECTORY(lib)

ADD_SUBDIRECTORY(test)
